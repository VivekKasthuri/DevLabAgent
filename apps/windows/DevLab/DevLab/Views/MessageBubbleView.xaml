<UserControl x:Class="DevLab.Views.MessageBubbleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Border x:Name="BubbleBorder" CornerRadius="8" Padding="10"
          Background="{StaticResource Bg3Brush}"
          BorderBrush="{StaticResource BorderBrush2}" BorderThickness="1">
    <StackPanel>
      <!-- Role label -->
      <TextBlock x:Name="RoleLabel" FontSize="10" FontWeight="SemiBold"
                 Margin="0,0,0,5" Opacity="0.8"/>
      <!-- Content -->
      <TextBlock x:Name="ContentText" FontSize="13" TextWrapping="Wrap"
                 Foreground="{DynamicResource TextBrush}" TextTrimming="None"/>
    </StackPanel>
  </Border>
</UserControl>
