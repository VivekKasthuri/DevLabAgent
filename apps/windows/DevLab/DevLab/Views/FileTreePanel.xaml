<UserControl x:Class="DevLab.Views.FileTreePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource Bg2Brush}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="36"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Header -->
    <Border Grid.Row="0" BorderBrush="{StaticResource BorderBrush2}" BorderThickness="0,0,0,1">
      <Grid Margin="12,0">
        <TextBlock Text="EXPLORER" FontSize="11" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextDimBrush}" VerticalAlignment="Center"/>
        <Button Content="↻" Style="{StaticResource IconButton}"
                HorizontalAlignment="Right" Click="OnRefresh"/>
      </Grid>
    </Border>

    <!-- Project label -->
    <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrush2}" BorderThickness="0,0,0,1" Padding="12,5">
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="📁" FontSize="11" VerticalAlignment="Center" Margin="0,0,5,0"/>
        <TextBlock x:Name="ProjectLabel" Text="No project open" FontSize="12" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
      </StackPanel>
    </Border>

    <!-- Tree -->
    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
      <TreeView x:Name="FileTree" Background="Transparent"
                BorderThickness="0" Padding="4"
                SelectedItemChanged="OnSelectedItemChanged">
        <TreeView.Resources>
          <Style TargetType="TreeViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextDimBrush}"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="Padding"    Value="2,2"/>
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Style.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#264F78"/>
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Bg3Brush}"/>
              </Trigger>
            </Style.Triggers>
          </Style>
        </TreeView.Resources>
        <TreeView.ItemTemplate>
          <HierarchicalDataTemplate ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="{Binding Icon}" Margin="0,0,5,0" VerticalAlignment="Center"/>
              <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
              <Ellipse Width="5" Height="5" Fill="{StaticResource AccentBrush}"
                       Margin="5,0,0,0" VerticalAlignment="Center"
                       Visibility="{Binding IsModified, Converter={StaticResource BoolVis}}"/>
            </StackPanel>
          </HierarchicalDataTemplate>
        </TreeView.ItemTemplate>
      </TreeView>
    </ScrollViewer>
  </Grid>
</UserControl>
